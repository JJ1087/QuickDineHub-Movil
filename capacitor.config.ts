import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.quickdh.quickdhmovil',
  appName: 'QuickDineHub-Movil',
  webDir: 'www'
};

export default config;
